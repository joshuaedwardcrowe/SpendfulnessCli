# Commands

How an instruction becomes a rendered table.

## Command

Taking given arguments to the handler.

## Command Handler

Using the command to orchestrate data into a view model then a console
table.
